import React, { useState } from 'react'
import { ethers } from 'ethers'

function FetchImage({provider,contract,account}) {
    const [imgurl,setImgurl] = useState("")
  return (
    <div>
        
    </div>
  )
}

export default FetchImage